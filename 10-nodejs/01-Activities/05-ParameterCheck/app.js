// console.log(parseFloat(process.argv[2]) + parseFloat(process.argv[3]));

// numbers

// num1 = parseFloat(process.argv[2]);

// num2 = parseFloat(process.argv[3]);

// if (num1 === num2) {
//     console.log("the numbers are equal");
// } else {
//     console.log("the numbers aren't equal");
// }

val1 = process.argv[2];

val2 = process.argv[3];

if (val1 == val2) {
    console.log("the values are the same");
} else {
    console.log("the values aren't the same");
}